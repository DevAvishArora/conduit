# Conduit

> Self-hostable workflow automation platform — backend engineering case study submission.

The full implementation lives on the [`feat/initial-implementation`](../../tree/feat/initial-implementation) branch and is described in the open Pull Request. The PR contains the comprehensive README (architecture, API reference, design decisions, manual testing guide, and full feature mapping against the brief).

🚧 This `main` is intentionally minimal until the PR is reviewed.
